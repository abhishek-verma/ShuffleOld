<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.NoActionBar.MainActivity" parent="AppTheme.NoActionBar">
        <item name="android:windowEnterTransition">@transition/main_activity_transition</item>
        <item name="android:windowExitTransition">@transition/main_activity_transition</item>
        <item name="android:windowReturnTransition">@transition/main_activity_transition</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_image_transform
        </item>
        <item name="android:windowSharedElementExitTransition">
            @transition/change_image_transform
        </item>
    </style>

    <style name="AppTheme.NoActionBar.PlayerActivity" parent="AppTheme.NoActionBar">
        <item name="android:windowEnterTransition">@transition/player_activity_transition</item>
        <item name="android:windowExitTransition">@transition/player_activity_transition</item>
        <item name="android:windowReturnTransition">@transition/player_activity_transition</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_image_transform
        </item>
        <item name="android:windowSharedElementExitTransition">
            @transition/change_image_transform
        </item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat" />


</resources>
